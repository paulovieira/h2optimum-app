require('./custom-css/app.css');

//var $ = require('jquery');
// add new ones (will be added dynamically as <style type="text/css">)

//require('./custom-css/material-dashboard-custom.css');


/*
// dynamically add the override stylesheet (available externally); we place it here
// (instead of being in the webpage) because this css might be adapted to each client
// on the fly (so it shouldn't be part of the bundles)

var $stylesOverride = $("<link>");
$stylesOverride.attr({
    type: 'text/css',
    rel: 'stylesheet',
    href: Host.areaFullPath + '/Static/styles-override.css'
});

$('head').append($stylesOverride); 
*/

